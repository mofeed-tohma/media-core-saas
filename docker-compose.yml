version: '3.8'

services:
  app:
    build: .
    container_name: media-saas-backend
    ports:
      - "8080:8080"
    volumes:
      - media_storage:/app/uploads
    restart: always

volumes:
  media_storage: